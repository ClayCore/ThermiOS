# Runtime Libraries

This directory is reserved for libraries used within the kernel code.
The standard C runtime library was reimplemented as a part of the process of cross-compiling without headers.
